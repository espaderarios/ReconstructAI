# Frontend Environment Variables (if needed)
# Note: VITE_API_URL is automatically set during GitHub Actions deployment
# In development, the Vite proxy (vite.config.ts) handles API calls to http://localhost:3000
